# nixpacks.toml for backend — tells Railway how to build the Spring Boot JAR
[phases.setup]
nixPkgs = ["jdk21"]

[phases.build]
cmds = ["./mvnw clean package -DskipTests -q"]

[start]
cmd = "java -Dserver.port=$PORT -jar target/college-discovery-1.0.0.jar"
